[package]
name = "dbrain-learn"
version.workspace = true
edition.workspace = true

[lib]
name = "dbrain_learn"
path = "src/lib.rs"

[dependencies]
deadlock-brain-core = { path = "../deadlock-brain-core" }
