[package]
name = "dbrain-normalize"
version.workspace = true
edition.workspace = true

[lib]
name = "dbrain_normalize"
path = "src/lib.rs"

[dependencies]
deadlock-brain-core = { path = "../deadlock-brain-core" }
