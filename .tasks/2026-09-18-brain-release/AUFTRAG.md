# Auftrag: Brain-Release abschließen

Nutzerauftrag: „Schließe alles ab, dass ich das deployen kann.“ Grundlage ist das Übergabeprotokoll vom 18.09.2026 mit U0 bis U8. Keine bloße neue Planung und keine Behauptung fachlicher Abnahme aus grünen Tests. Der Orchestrator führt reale Datenprüfungen, unabhängigen Primärdatenlauf, Review, Migration und zulässigen Deploy in derselben Sitzung aus.

## Arbeitsstand und Besitz

Du bist der einzige Coding-Thread für dieses Paket. Keine Unter-Threads oder Unter-Agenten spawnen. Intent ist diese externe ChatGPT-Sitzung; keine T3-Intent-ID vorhanden. Benutze ab jetzt ausschließlich `/home/nathanael/.worktrees/brain-release-20260918`, Branch `codex/brain-release-20260918`, Basis `9efeb1e44ead5cdf5d01e05f242291fee79e803e`. Der bisherige U0-Worktree bleibt unangetastet. Nur Feature-Branch committen/pushen. Kein eigener Merge, keine Produktionsmigration, keine laufenden Dienste oder Konfigurationen ändern. Der Orchestrator übernimmt Betrieb und unabhängiges Review. Lokale Regeln beachten; Runtime und neue Werkzeuge Rust, Postgres, keine Secret-/ENV-Dateien öffnen oder Werte loggen.

## Zuerst implementieren und pushen: konkrete Release-Blocker

Lies `.tasks/2026-09-18-brain-evidence-u0/INTEGRATION.md`, REPORT und `docs/AUTONOMOUS_PATCH_REVIEW.md` sowie die tatsächlichen Rust-Pfade. Graphify-Bestandssuche vor Neubau (absoluter CLI-Pfad `/home/nathanael/.local/bin/graphify`). Das globale Graphfile ist lesbar. Wiederverwenden statt parallele Plattform.

1. **Kontextauswahl für den echten unabhängigen Lauf.** Der aktuelle Kontext umfasst 1518 vor Veröffentlichung beobachtete Assets-Snapshots; die Rohpayloads passen nicht in 768000 Bytes. Nicht einfach Budget/Limit erhöhen und nichts still abschneiden. Implementiere eine deterministische, nachvollziehbare Gameplay-Projektion und Auswahl: kompletter Patchoriginaltext und alle Events bleiben; ein kompakter Entity-/Abhängigkeitskatalog soll auch nicht direkt im Patch genannte Helden/Items sichtbar halten. Detailquellen über Patch-Entities, tatsächliche Hero-/Ability-/Item-Referenzen und allgemeine Mechanikbezüge auswählen. Keine aus dem Vergleichsvideo abgeleiteten Zielantworten oder Hero-Sonderfälle einbauen. Ausgeschlossene Felder/Entities, Gründe, Auswahlversion und Umfang offen im Kontext ausweisen. Alle übernommenen Werte behalten Snapshot-ID, Inhaltshash und JSON-Pfad; unbestätigte Vorversion bleibt unbekannt. Kein String-Kürzen mitten im Beleg und kein patch-only als Vollabnahme. Ein Versions-/Dimensionsproblem bleibt explizit offen. Erst reale Payloadstruktur anschauen, nicht API-Schema erfinden. Zentraler AiClient/Modell/Timeout bleibt unverändert.
2. **Patchrevisionen tatsächlich importieren.** Vorhandenen Rust-Import von `patchnotes.changelog_posts` in `brain.patch_events` prüfen: gleiche Quell-ID mit geändertem raw_content muss erkannt und revisionssicher synchronisiert werden, einschließlich nicht mehr enthaltener Events. Quellzeilen nicht manuell zurechtbiegen. Kein neuer Sammler. Betrieb startet aktuell `/home/nathanael/.local/bin/deadlock-brain-patchnotes-sync.sh`; Orchestrator liest diesen Wrapper und stellt Details bereit. Erstelle nötigen sicheren Rust-CLI-Sync- bzw. Prüfbefehl im bestehenden Brain, der in den Timerweg integriert werden kann. Unveränderte Quellen dürfen keine erneuten teuren Analysen auslösen. Aktualisierte Quellen dürfen keine alte Erkenntnis als aktuell geprüft durchreichen.
3. **Geteilte Historie nutzbar machen.** Prüfe bestehenden `mcp/server.py`-Pfad und die zusätzliche Branch `origin/fix/patch-insights-evidence-20260918` nur lesend, um vorhandene Entwürfe nicht doppelt zu bauen. Neues Runtime-Modul Rust-nativ, vorhandene CLI-Historienabfrage in wiederverwendbares Modul auslagern; als lesendes Tool an den tatsächlichen Brain-MCP-/Bot-Zugangsweg anbinden. Keine neuen pauschalen PUBLIC-Rechte oder offenen Netzwerkports. Aliasauflösung über tatsächliche Entity-Aliase, natürlich formulierte Datumsfragen ohne Analysemodell, saubere Publikations-/Beobachtungs-/Baseline-Semantik und sichtbare Trefferbegrenzung. Keine falsche Volltext-QA-Abnahme. Prüfe, dass gelöschte oder revalidierungsbedürftige Ableitungen nicht als aktuelle Fakten erscheinen.

## Tests und Zwischenübergabe

Neue Fehlerfälle erst als rote Gegenprobe zeigen. Alle betroffenen Rust-Tests, clippy und nur eigene Dateien formatieren. Die vier bislang ignorierten YT-Integrationstests nach Möglichkeit gegen ein richtiges Scratch-Schema ausführen, nicht still skippen. Keine Fixture auf Produktions-DB. Eigener Build mit höchstens zwei Jobs; bestehendes target-u0 darf nicht konkurrierend beschrieben werden.

Nach funktionierendem Kontext/Synchronisationspfad unmittelbar lauffähigen Feature-Commit pushen und REPORT schreiben, damit der Orchestrator U1 ausführen kann. Keine weiteren Modellläufe, Creator-Transkripte oder Referenzvideo vor dem gesicherten unabhängigen Lauf. Weitere U2-U5-Modelle werden anschließend aus den echten Lücken abgeleitet; keine vorgefertigten Creator-Urteile.

## Abnahmegrenzen

Es ist ausdrücklich noch kein Nachweis autonomen Spielverständnisses, wenn nur Kontext/Prompt/Historie/Tests funktionieren. Entwürfe bleiben ungeprüft. U7 verlangt tatsächliche Bildbelege, U8 ein abspielbares Video; weder JSON noch Storyboard als Video melden. Öffentliche Veröffentlichung/Uploads sind nicht beauftragt. Parallele RAG-Pakete A-E und andere Reasoner-Feature-Worktrees nicht ändern oder eigenmächtig mergen.
