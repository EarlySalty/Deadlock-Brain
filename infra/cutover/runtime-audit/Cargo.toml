[package]
name = "brain-runtime-audit"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
publish = false

# Read-only, operator-invoked S11 preparation tool. Not a production workspace member.
# Deliberately no third-party dependencies and no persistent state.
