[package]
name = "dbrain-s10-evals"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
publish = false

# Offline preparation tool owned by S10, not a production workspace member.
[workspace]

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.150"
sha2 = "=0.10.9"
