# CR-S10-001: Integration der unabhängigen Prüfungen

An: Chat 00 und Chat 02; Folgeinputs von 01, 03, 07, 08, 09 und 11–14.

Status: beantragt, nicht freigegeben. Basis `30326512568b7370524956839100462ba71bdb92`.

## Anlass und Pfadgrenzen

S10 besitzt `architecture/migration/evals/**` und `architecture/migration/benchmarks/**`. Der integrierte Status erlaubt Vorbereitung. Gemeinsame Contracts, Schema und G0–G3 fehlen noch. Das Werkzeug ist innerhalb der S10-Pfade implementiert, ohne fremde Module oder Root-Manifeste zu ändern. Koordinationsstatus und Freigaben werden nicht von S10 selbst gesetzt.

## Benötigte Entscheidungen

1. **02 / CI:** Den vorhandenen deterministischen PR-Pfad um Format-, Clippy-, Test- und Build-Prüfungen für `architecture/migration/evals/Cargo.toml` ergänzen und die vollständige Pflichtliste entsprechend aktualisieren. Befehle stehen in `README.md`. Ein grünes `cargo check` für `rust/` prüft S10 nicht. Keine neue konkurrierende Gate-Verwaltung; gleiche secretlose Runner und gleiche Vertrauensgrenzen verwenden.
2. **00 / Profil:** Contract-, DB-, Knowledge- und Quellenstände sowie tatsächliche Zielhardware, Daten-/Indexgrößen, Zielrate, Parallelität, Lastdauer, absolute SLOs und Qualitätsgrenzen vor Optimierungen festlegen. `profile.json` bleibt bis dahin ausdrücklich nicht freigegeben; ungemessene Werte bleiben `null`.
3. **02 / 07 / 08 / 09:** Gemeinsame Testzugänge für Caller-Kontext, Objekt-ACL, Citation-Zugriff, Stufentraces und Providerfehler integrieren. S10s Berichtformat ist nur ein Offline-Artefakt, keine Ersatz-API für fehlende gemeinsame Contracts.
4. **01 / 09:** Die 224 vorhandenen Support-Fälle am festgehaltenen Docs-Commit in die echte Consumer-Evaluation einbinden. S10 hat Dateien und Labels geprüft, aber keine Antworten erzeugt und keine Supportqualität gemessen.
5. **03 / 11:** Isolierte Stagingdaten, geprüfte Quellenrechte, Restore-Snapshot, erlaubte Writer-Cutover-/Rollback-Proben und Rust-only-Testumgebung bereitstellen. Keine Änderungen an Produktionsdaten für Tests.
6. **05 / 12 / 13 / 14:** Unabhängige Domain-/Wiki-/Schema-/Replay-Referenzen, vollständige Pflichtklassen und verfügbarkeitskorrekte Population-Splits liefern. Die Szenariospezifikationen ersetzen diese Datensätze nicht.

## Integrationsabnahme

Geheimnislose S10-Prüfungen müssen im aktuellen PR-Kontext verpflichtend laufen. Entfernung eines Pflichtchecks, absichtlicher Testfehler, unerwarteter Skip und veralteter Commit dürfen nicht grün werden. Erst danach und mit freigegebenem Gesamtprofil folgen echte Messläufe. S10 aktiviert keine automatischen Merges, Produktions-Deployments oder G0–G4-Freigaben.
